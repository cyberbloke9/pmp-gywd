'use strict';

/**
 * GYWD Dashboard
 *
 * Visual dashboard components for project status and metrics.
 */

const { DashboardRenderer, DASHBOARD_THEME, CHART_TYPE } = require('./dashboard-renderer');

module.exports = {
  DashboardRenderer,
  DASHBOARD_THEME,
  CHART_TYPE,
};
