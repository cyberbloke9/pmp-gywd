'use strict';

/**
 * GYWD Index
 *
 * Indexing utilities for fast lookups.
 * Zero external dependencies.
 */

const { KeywordIndex, keywordIndex } = require('./keyword-index');

module.exports = {
  KeywordIndex,
  keywordIndex,
};
